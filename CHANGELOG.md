# 更新日志

## v2.2.1 - 2026-08-18

### 修复

- 新增确定性进度条脚本，直接输出当前值、目标值、百分比和固定 40 格满量程，避免模型只按当前值连续增加字符。
- 未达标时固定保留 `░` 空余段；超过 100% 时在完整 40 个 `█` 后使用 `▣` 显示超额，超过 150% 时截断超额段。
- 普通餐食必须原样使用脚本结果，脚本失败时明确提示，不再临时手画不同样式。

## v2.2.0 - 2026-08-17

### 调整

- 普通餐食的热量、蛋白质和碳水进度条统一以 40 格表示 100%，避免不同回复之间长度漂移。
- 超过 100% 时按同一刻度追加可区分的超额段并标注实际百分比；超过 150% 时截断超额段并显示 `>150%`，避免超额结果仍看起来接近满格。

## v2.1.0 - 2026-08-16

### 新增

- 餐食估算和饮食账本增加脂肪下限、上限与采用值；脂肪只在记录和按需详细分析中出现，不新增每日/月度脂肪目标、进度或报告图表。
- 新增热量目标规划器，对照 Schofield、Mifflin、Liu、Xue1 四种公式，检查公式离散度、目标 BMI、期限强度和最低摄入边界，并给出可选择的调整方案。
- 新增本地食物查询：随包提供台湾 FDA 政府开放数据的 2181 种食物宏量营养精简表，包含来源、开放许可、原始与产物校验值和地区局限说明。
- 首次和月度复核优先使用原生选择组件；不可用时一次成组提问，避免机械地逐题往返。

### 调整

- 普通餐食回复收敛为本餐采用值、热量/蛋白质/碳水今日进度、一个建议和写入状态；详细食物表、误差与体重预测仅在分析或日总结时展开。
- 食物匹配遵循“用户称量/包装 > 唯一本地匹配 > 经许可单次查询 > 图片估算”；候选不唯一时必须让用户确认。
- 18～59 岁以 Schofield 作为初始中心值并与其他公式对照；离散度大于 10% 或年龄 60 岁以上时降低可信度，后续以真实体重趋势保守校准。

### 兼容与迁移

- 新增 19→22 列原子迁移命令；三列脂肪字段插在采用碳水之后，历史记录原有 19 个单元格保持不变，新增字段留空。
- 已是 22 列的账本重复迁移不会改写；备份只进入系统临时目录，并按账本路径隔离，避免多账本或测试互相覆盖。
- 周/月报告继续只展示热量、蛋白质和碳水目标，不因账本新增脂肪列而扩张。

## v2.0.1 - 2026-08-09

### 修复

- 重绘训练部位图：使用清晰的人体轮廓、标准正面/背面标识、表面肌群分区和直接引导线，避免原几何块状图难以辨认位置。
- `render` 默认改为系统临时目录中的自包含 HTML，并固定覆盖同名文件，不再要求把 SVG 写入健康项目或当前工作目录。
- 保留显式 `.html`/`.svg` 导出能力；只有用户明确要求长期保存时才使用指定路径。

### 兼容

- 训练账本、饮食摘要、恢复窗口、滚动计划和 10 个部位的数据键保持不变。
- 解剖底图为仓库内原创矢量模板；第三方资料仅用于核对方向、命名和表面肌群位置。

## v2.0.0 - 2026-08-09

### 新增

- 将饮食减脂、力量训练、有氧训练、恢复判断和滚动计划合并为一个 Skill 入口。
- 在现有 `减脂档案.md` 中增加训练目标、经验、器械、可用时长、频率范围、限制和偏好；月初与体重联合复核。
- 新增 `training_tracker.py`：支持训练配置、动作级 CSV 落账、恢复反馈、行动分档、滚动 A/B 计划和确定性 SVG 部位图。
- 新增计划达标比较：按动作、组数、次数和 RIR 判断达标/偏轻/偏重，并限制下次只调整一个变量。
- 使用唯一训练 ID 幂等同步训练明细与 19 列饮食账本摘要；第二步失败可用同一 ID 补写，不重复训练数据。
- 新增 10 个可行动训练部位及常见力量/有氧动作映射；未知动作必须显式提供部位，不允许猜测。
- 新增力量、有氧、双账本、恢复、计划和渲染自动化测试。

### 调整

- 不再假定固定训练日或每周次数；按最近 28 天历史、实际可用分钟数和恢复状态选择下一节。
- 恢复结果改为宽复查窗口、行动状态和可信度，明确不是恢复百分比或医学测量。
- 常规训练继续纳入长期活动水平，不按设备热量重复增加进食额度；仅明确额外有氧沿用保守弹性规则。
- 普通餐食仍保持 v1.1.0 的结果优先回复合同，不加载训练账本或训练参考。

### 兼容与迁移

- 原 `饮食记录.csv` 19 列表头、体重预测脚本、周/月报告和月度体重规则保持兼容。
- 首次使用训练功能时才创建 `训练记录.csv`；不反向补造历史训练。
- 已有 `减脂档案.md` 在首次训练配置时新增一个“训练配置”小节，不创建第二份个人档案。

### 已知边界

- 训练与恢复规则仅适用于 18 岁以上普通成年人的一般性记录，不替代疾病管理、伤病诊断或康复处方。
- 人体 SVG 的颜色表示训练刺激分档，不表示肌肉增长、热量消耗或恢复程度。

## v1.1.0 - 2026-08-07

- 每餐回复改为“采用值与条件性预计体重变化 → 今日进度 → 一个建议 → 详细摄入 → 总结计算 → 写入状态”。
- 估算区间、可信度和主要误差后置；保留可见进度条和详细食物表。

## v1.0.0 - 2026-08-04

- 发布经回退验证的 V4 基线：19 列饮食账本、月度体重、确定性体重模型和 HTML 周/月报告。
