---
name: diet-fat-loss-tracker
description: "Use when 用户需要统一管理成人减脂饮食、力量训练、有氧训练、训练部位与恢复、滚动健身计划、训练提醒、餐食或运动落账、首次或月初联合更新体重/摄入/训练目标，或生成周报、月报、HTML/SVG 进度报告。普通餐食只走饮食流程；只有用户提到训练、可训练时间、恢复或计划时才加载训练规则。"
---

# 减脂饮食与训练记录

## 按需读取

先定位使用者指定的 `减脂档案.md`、`饮食记录.csv` 和可选 `训练记录.csv`。

- 普通餐食：只读取档案“当前目标”、饮食 CSV 表头和当天记录；不读取训练账本、训练参考或报告代码。
- 首次、月初、档案/表头缺失或用户要求重算：读取 [建档与目标规则](references/profile-and-targets.md)。
- 用户报告训练、询问今天练什么、部位恢复或训练提醒：读取 [训练、恢复与滚动计划](references/training-and-recovery.md)，运行 `scripts/training_tracker.py`，不要自行重算脚本结果。
- 用户追问体重模型或要改公式：才读取 [体重变化模型](references/weight-change-model.md)。

档案未确认适用人群边界时，按首次使用处理。不得依赖聊天记忆补造资料或记录。

## 记录饮食

1. 根据截图及可选文字识别食物和份量，分别给出热量、蛋白质、碳水的合理区间、采用中值、可信度（高/中/低）和主要误差。
2. 没有文字时直接估算。仅当是否吃完、份量、含糖饮料、隐藏主食、明显油或酱汁会改变当日判断时，一次性询问全部必要信息。
3. 每种食物追加一行，采用值用于汇总。更正前核对日期、餐次、食物；多条匹配时先询问，不按行号猜测，不重复新增。
4. 写入后回读对应行并核对列和值。只有回读成功才写“已写入”；无法读写时写“未写入：原因”，并给出待写入数据。

仅有简单运动摘要且用户不需要训练分析时，可沿用饮食账本的一行运动记录。存在动作、组数、重量、RIR/RPE、部位、恢复或计划信息时必须走“记录训练”，由训练脚本同步饮食摘要，禁止手工重复追加。常规运动不改变当天目标；明确属于额外有氧时才按参考文件增加弹性额度。设备消耗只记入备注。

回读成功后运行 `python3 scripts/weight_forecast.py daily YYYY-MM-DD`；路径不在默认位置时用 `--help` 查看覆盖参数。只读取其一行 JSON，不读取脚本。`status=ok` 时把区间写入“今日累计”；否则写“证据不足：原因”，不得自行改用目标热量推算。

## 记录训练

1. 若档案没有“训练配置”，一次性询问主要目标、训练经验、器械条件、单次可用时长、可训练频率范围、伤病与限制、不喜欢动作；频率允许“不固定”。运行 `training_tracker.py configure` 写入并回读，不建立第二份个人档案。
2. 把本次训练整理成动作 JSON。已知动作使用脚本映射；未知动作不得猜部位，只有用户或可靠资料明确主要/次要部位后才能传入 `primary`/`secondary`。
3. 本次结束后只补一个整体选择：偏轻/刚好/偏重/疼痛或异常。用户已表达时不重复询问。力量动作尽量记录组数、次数、重量和 RIR；有氧记录时长和 RPE/谈话强度。缺失值保持空白。
4. 运行 `python3 scripts/training_tracker.py record ...`。脚本先幂等写入训练明细，再用同一训练 ID 向饮食账本写摘要。只有两份回读均成功才说“已同步”；`status=partial` 必须原样说明并用相同训练 ID 重试，不得重新新增。
5. 本次来自脚本计划时，把原 `plan` JSON 与训练 ID 传给 `evaluate`，用其结果判断是否完成至少 80%、偏轻/偏重及下一次只调整哪一个变量；不得凭印象另算。
6. 训练后运行 `status` 和不带 `--output` 的 `render`。读取其返回的临时 HTML 路径并展示；默认文件固定覆盖在系统临时目录，不得把部位图写进健康项目、个人数据目录或当前工作目录。只有用户明确要求长期保存或导出时才传 `--output`。人体图只显示训练刺激；恢复只显示下一次重训复查窗口、行动状态和可信度，不得改写成恢复百分比或精确完成时间。
7. 用户反馈恢复时只询问已训练或下一节相关部位，可整体选择“正常/轻微疲劳/明显影响动作/疼痛”，再运行 `feedback`。少于 3 次同类反馈不永久校准。

## 今天能训练

用户说今天有时间、问下一节或临时增加训练时，不假定固定星期或每周次数。确认本次可用分钟数；已有可靠值时不重复询问。运行 `training_tracker.py plan`，按滚动 A/B 队列、最近 28 天训练和当前恢复选择下一节：

- 本周只能练一次也优先覆盖主要肌群；临时能练多次就继续队列，不重置自然周。
- 相关部位未到重训窗口时降量、改练或安排低强度有氧，不为凑计划强行训练。
- 力量和有氧不互换训练量；常规训练不重复提高饮食额度。
- 计划是一般性建议。疼痛、明显动作异常或疾病状态优先停止并建议专业评估。

## 初次与月度联合复核

首次同时完成减脂资料和训练配置。月份变化时，在现有体重问题后追加一个合并问题：“训练目标、可用时间范围、器械、伤病/疼痛是否变化？”用户回答“没变化”时完整保留训练配置，不逐项重问；有变化时只更新变化项。月度可训练范围用于规划，不转成固定星期表。

## 提醒

只有用户明确要求提醒并确认时间后才创建或修改任务。优先最多两个：月初联合复核、可选的低频训练机会检查。机会检查读取训练账本和恢复状态，只有存在值得执行的下一节且当天未记录时才提醒；不创建每天一个独立任务，不把提醒时间等同于必须训练时间。创建前先在当前对话手工试跑一次提示内容。

## 周报与月报

用户要求周报、月报或 HTML 进度报告时，不走每日回复模板，也不自行重写页面。运行 `python3 scripts/generate_report.py week|month --date YYYY-MM-DD`；路径不在默认位置时用 `--help` 查看覆盖参数。脚本内置页面、计算和缺失数据规则，不要把脚本或 HTML 读入聊天上下文。

命令成功后回读生成文件，只回复报告路径和一句数据完整性提示；失败则回复原始错误，不声称已生成。普通饮食记录不得运行或读取报告脚本。

## 回复

按以下内容顺序回复，不在结果区前增加铺垫、公式、内部推理、完整历史或额外科普。

### 1. 本餐结果

只显示本餐热量、蛋白质、碳水的采用值，以及“若今天此后不再进食”的预计体重变化。预计体重变化必须标明是理论组织能量等价、低可信，不得暗示为明早秤重；脚本返回证据不足时直接显示原因。此处不显示估算区间、可信度、误差或计算过程。

### 2. 今日进度

显示热量、蛋白质、碳水的今日累计采用值、当前目标、完成百分比、进度条和偏低/合适/偏高判断。必须有可见进度条，但不限定字符、长度、颜色或组件样式。百分比可以超过 100%；热量使用名义目标，蛋白质和碳水只有区间时以下限作为 100%，判断仍按完整区间。

### 3. 建议

只给一个最值得执行的下一餐动作；不要求为凑数字强行进食，不把今天的缺口留到明天补偿。

### 4. 详细摄入

使用表格列出本餐每种食物、估算份量、热量/蛋白质/碳水的估算区间和采用值、可信度；必要时写主要误差。

### 5. 总结计算

汇总本餐热量、蛋白质、碳水的合计区间和采用值，并简述影响估算的主要误差。这里才展开区间和计算依据，且必须与结果区、详细摄入和今日累计一致。

### 6. 记录状态

最后简短说明“已写入”或“未写入：原因”。未写入时附待写入数据；没有回读证据不得声称已写入。

上述内容顺序固定，具体视觉形式由 AI 根据当前工具能力自行决定。可以使用 Markdown、原生卡片或图表；判断能明显改善可读性且当前环境适合时，也可以输出简洁、自包含的 HTML、CSS 或 JS。不要为了炫技增加无关内容或牺牲日常回复的简洁性。

## 最终检查

- 区间、采用值、本餐合计和今日累计一致；保留截图、油和酱汁的不确定性。
- 条件体重变化必须写“若今天此后不再进食”，不得改写为明早秤重或长期减脂承诺。
- 只评价当天是否接近当前目标；长期效果只在月度实测对比中判断。
- 没有实际回读证据时，不得声称已写入。
